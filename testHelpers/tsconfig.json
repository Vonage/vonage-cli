{

  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../tsconfig.json",

  "compilerOptions": {
    "module": "ES2022"
  },

  "exclude": [
    "__tests__",
    "jest.config.js",
    "dist"
  ],

  "ts-node": {
    "esm": true
  }
}
