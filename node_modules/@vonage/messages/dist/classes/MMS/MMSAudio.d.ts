import { AbstractAudioMessage } from '../AbstractAudioMessage';
import { MMSAudioInterface } from '../../interfaces';
import { MessageParamsAudio } from '../../types';
export declare class MMSAudio extends AbstractAudioMessage implements MMSAudioInterface {
    channel: 'mms';
    constructor(params: MessageParamsAudio);
}
