import { AbstractFileMessage } from '../AbstractFileMessage';
import { WhatsAppFileInterface } from '../../interfaces';
import { WhatsAppFileParams } from '../../types';
export declare class WhatsAppFile extends AbstractFileMessage implements WhatsAppFileInterface {
    channel: 'whatsapp';
    constructor(params: WhatsAppFileParams);
}
