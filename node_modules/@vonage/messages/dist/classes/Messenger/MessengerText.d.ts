import { AbstractTextMessage } from '../AbstractTextMessage';
import { MessengerTextInterface } from '../../interfaces';
import { MessengerTextParams, MessengerType } from '../../types';
export declare class MessengerText extends AbstractTextMessage implements MessengerTextInterface {
    channel: 'messenger';
    messenger: MessengerType;
    constructor(params: MessengerTextParams);
}
