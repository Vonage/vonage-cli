import { AbstractImageMessage } from '../AbstractImageMessage';
import { MessengerImageInterface } from '../../interfaces';
import { MessengerImageParams } from '../../types';
export declare class MessengerImage extends AbstractImageMessage implements MessengerImageInterface {
    channel: 'messenger';
    messenger: any;
    constructor(params: MessengerImageParams);
}
