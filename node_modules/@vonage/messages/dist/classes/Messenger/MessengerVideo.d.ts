import { AbstractVideoMessage } from '../AbstractVideoMessage';
import { MessengerVideoInterface } from '../../interfaces';
import { MessengerType, MessengerVideoParams } from '../../types';
export declare class MessengerVideo extends AbstractVideoMessage implements MessengerVideoInterface {
    channel: 'messenger';
    messenger: MessengerType;
    constructor(params: MessengerVideoParams);
}
