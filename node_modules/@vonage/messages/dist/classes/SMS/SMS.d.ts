import { AbstractTextMessage } from '../AbstractTextMessage';
import { SMSTextInterface } from '../../interfaces';
import { MessageParamsText } from '../../types';
export declare class SMS extends AbstractTextMessage implements SMSTextInterface {
    channel: 'sms';
    constructor(params: MessageParamsText | string, to?: string, from?: string, clientRef?: string);
}
