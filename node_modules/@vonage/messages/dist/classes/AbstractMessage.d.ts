import { MessageInterface } from '../interfaces';
import { MessageParams } from '../types';
export declare abstract class AbstractMessage implements MessageInterface {
    to: string;
    from: string;
    clientRef?: string;
    constructor(params: MessageParams);
}
