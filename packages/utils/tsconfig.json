{
    "extends": "../../tsconfig.json",
    
    "compilerOptions": {
        "rootDir": "src",
        "outDir": "dist",
    },

    "exclude": [
        "__tests__",
        "dist"
    ],

    "ts-node": {
        "esm": true
    }
}
