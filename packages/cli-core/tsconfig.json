{
  "extends": "../../tsconfig.json",

  "compilerOptions": {
    "rootDir": "lib",
    "outDir": "dist",
    "baseUrl": "."
  },

  "includes": [
    "lib"
  ],

  "exclude": [
    "dist",
    "__tests__"
  ]
}
