{
    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "rootDir": "lib",
        "outDir": "dist"
    },

    "exclude": [
        "__tests__",
        "dist",
        "bin/*"
    ],

    "ts-node": {
        "esm": true
    }
}
