{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../tsconfig.json",

  "compilerOptions": {
    "rootDir": "bin",
    "outDir": "dist/lib"
  },

  "includes": [
    "bin"
  ],

  "references": [
    { "path": "../cli-jwt" },
    { "path": "../cli-core" }
  ],

  "exclude": [
    "dist",
    "__tests__"
  ],

  "ts-node": {
    "esm": true
  }
}
