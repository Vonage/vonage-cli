{
  "extends": "../../tsconfig.json",

  "compilerOptions": {
    "rootDir": "bin",
    "outDir": "dist",
    "baseUrl": "."
  },

  "includes": [
    "bin"
  ],

  "exclude": [
    "dist",
    "__tests__"
  ],

  "ts-node": {
    "esm": true
  }
}
