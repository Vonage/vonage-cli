{
    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "rootDir": "bin",
        "outDir": "dist"
    },

    "exclude": [
      "bin",
      "__tests__",
      "dist"
    ],

    "references": [
        { "path": "../core" }
    ],

    "ts-node": {
        "esm": true
    }
}
